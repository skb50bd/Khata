@using Khata.DTOs
@model IEnumerable<SaleDto>

<table class="table table-sm table-borderless table-responsive-sm table-hover table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.Customer.FullName)</th>
            <th>@Html.DisplayNameFor(model => model.Type)</th>
            <th class="text-right">@Html.DisplayNameFor(model => model.PaymentTotal)</th>
            <th class="text-right">@Html.DisplayNameFor(model => model.PaymentDue)</th>
            <th class="text-right">@Html.DisplayNameFor(model => model.Profit)</th>
            <th class="text-right">Items</th>
            <th class="text-right">Created At</th>
            <th class="text-right">Created by</th>
            <th class="text-right"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="js-clickable-row"
                data-href="@Url.Page("/Sales/Details", new { id = item.Id })">
                <td>@Html.DisplayFor(modelItem => item.Id)</td>
                <td>@Html.DisplayFor(modelItem => item.Customer.FullName)</td>
                <td>@Html.DisplayFor(modelItem => item.Type)</td>
                <td class="text-right">@Html.DisplayFor(modelItem => item.PaymentTotal)</td>
                <td class="text-right">@Html.DisplayFor(modelItem => item.PaymentDue)</td>
                <td class="text-right">
                    @if (Context.User.IsInRole("Admin"))
                    {
                        @Html.DisplayFor(modelItem => item.Profit)
                    }
                    else
                    {
                        @($"{0:c2}");
                    }
                </td>
                <td class="text-right">@Html.ValueFor(modelItem => item.Cart.Count)</td>
                <td class="text-right">@Html.ValueFor(modelItem => item.Metadata.CreatedAt)</td>
                <td class="text-right">@Html.DisplayFor(modelItem => item.Metadata.Creator)</td>
                <td class="text-right">
                    <a asp-page="../Invoices/Details"
                       asp-route-id="@Html.ValueFor(modelItem => item.InvoiceId)"
                       class="card-link btn btn-sm btn-inline btn-success blank-link">Invoice</a>
                </td>
            </tr>
        }
    </tbody>
</table>