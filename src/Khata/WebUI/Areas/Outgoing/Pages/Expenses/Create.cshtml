@page
@model Expenses.CreateModel

@{
    ViewData["Title"] = "New Expense";
}

<div class="row">
    <div class="col-md-4">
        <form method="post" class="immutable-submit" 
              autocomplete="on">
            <partial name="_ExpenseForm" for="ExpenseVm"/>

            <div class="form-group">
                <input type="submit" 
                       value="Create" 
                       class="btn btn-primary btn-lg" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
