@page
@using WebUI.Pages.DebtPayments.Components.DebtPaymentInfo
@model WebUI.Pages.DebtPayments.DetailsModel

@{
    ViewData["Title"] = "Debt Payment Details";
}

@await Component.InvokeAsync(
    nameof(DebtPaymentInfo),
    new { debtPayment = Model.DebtPayment }
    )
