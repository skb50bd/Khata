@page
@using WebUI.Pages.Expenses.Components.ExpenseInfo
@model WebUI.Pages.Expenses.DetailsModel

@{
    ViewData["Title"] = "Expense Details";
}

@await Component.InvokeAsync(
    nameof(ExpenseInfo),
    new { expense = Model.Expense }
    )
