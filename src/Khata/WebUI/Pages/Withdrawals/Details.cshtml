@page
@model WebUI.Pages.Withdrawals.DetailsModel
@{
    ViewData["Title"] = "Withdrawal";
}

<h1>Deposit</h1>

<div class="card" style="width:40rem">
    <div class="card-header">
        <div class="col-6">Deposit# @Model.Withdrawal.Id</div>
    </div>
    <div class="card-body">
        <h3 class="card-title">@Model.Withdrawal.TableName</h3>
        <table class="table table-sm table-hover mt-5">
            <tbody>
                <tr>
                    <th>Amount</th>
                    <td>@Html.DisplayFor(model => model.Withdrawal.Amount)</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>@Html.DisplayFor(model => model.Withdrawal.Description)</td>
                </tr>
            </tbody>
        </table>
        <hr />

    </div>

    <div class="card-footer">
        <partial name="_UpdateSummary" for="Withdrawal.Metadata" />
    </div>
</div>

